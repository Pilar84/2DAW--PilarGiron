services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MAILEROO_TOKEN: ${MAILEROO_TOKEN}
      MAILEROO_FROM_ADDRESS: ${MAILEROO_FROM_ADDRESS}
      DJANGO_DEBUG: "true" 
    depends_on:
      - db
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000


  frontend:
    image: javier08877/steamlike:4.0
    ports:
      - "3000:80"
    environment:
      BACKEND_URL: http://localhost:8000
    depends_on:
      - web

volumes:
  postgres_data:
